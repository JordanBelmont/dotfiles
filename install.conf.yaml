- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.p10k.zsh:
    ~/.gitconfig:
    ~/.config/bat:
      create: true
      path: config/bat

- create:
    - ~/Work

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true